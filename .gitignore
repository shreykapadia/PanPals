# === Secrets — NEVER commit these ===
.env
.env.local
.env.*.local
*.key
*.pem
.mcp.json
.zshrc

# === Node ===
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# === Expo / React Native ===
.expo/
.expo-shared/
dist/
web-build/
expo-env.d.ts

# Native build folders (only present in bare/prebuild)
/ios/
/android/
*.orig.*
*.jks
*.p8
*.p12
*.mobileprovision

# === Supabase (local stack) ===
supabase/.branches/
supabase/.temp/

# === Catalog seed source data (D17) ===
# The raw third-party CSV is an input to scripts/build-catalog-seed.mjs, not
# an artifact — regenerate supabase/seed.sql from it, don't commit it.
data/

# === Testing / coverage ===
coverage/
.maestro/.cache/

# === Local working notes (not for the team) ===
SESSION-HANDOFF.md

# === Editor / OS junk ===
.DS_Store
*.log
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

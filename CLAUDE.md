# CLAUDE.md

1. Read `AI-CONTEXT.md` in full before doing anything. It is the source of truth — it outranks any remembered context or agent memory.
2. Obey the file-ownership matrix in AI-CONTEXT.md §3 — never edit paths you don't own.
3. If a task requires touching another lane, stop and output a `CROSS-LANE REQUEST` instead.
4. Never run `git push --force`, never merge, never commit directly to `main`.

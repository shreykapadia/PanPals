#!/usr/bin/env bash
# PLACEHOLDER (Phase B1). The real two-user RLS verification harness
# (supabase/tests/rls_verification.sql) lands in Phase B5, along with the
# db-reset + psql wiring this script will run. Every schema/* PR must attach
# this script's PASS output once B5 lands (AI-CONTEXT §7 / DATA-MODEL.md).
set -euo pipefail

echo "rls-check.sh is a placeholder until Phase B5 — no verification SQL exists yet."
